import 'package:flutter/material.dart';

class SpaceColors {
  /// Deep Space Black (Canvas base)
  static const Color spaceBlack = Color(0xFF06070D);
  
  /// Midnight Blue (Cards, panels, secondary backgrounds)
  static const Color midnightBlue = Color(0xFF0F1222);
  
  /// Neon Cyan (Accent, focus, primary action glows)
  static const Color neonCyan = Color(0xFF00F2FE);
  
  /// Electric Purple (Highlights, reminders, AI indicators)
  static const Color electricPurple = Color(0xFF9D4EDD);
  
  /// Frosted Glass base
  static const Color glassWhite = Color(0x1FFFFFFF);
  
  /// Text colors
  static const Color textPrimary = Color(0xFFF3F4F6);
  static const Color textSecondary = Color(0xFF9CA3AF);
  
  /// Category visual coding colors
  static const Color education = Color(0xFF3B82F6);       // Blue
  static const Color career = Color(0xFF10B981);          // Green
  static const Color health = Color(0xFFEF4444);          // Red
  static const Color finance = Color(0xFFF59E0B);         // Amber
  static const Color personal = Color(0xFFEC4899);        // Pink
  static const Color important = Color(0xFF8B5CF6);       // Purple
  static const Color savedLinks = Color(0xFF06B6D4);      // Cyan
  static const Color aiGenerated = Color(0xFFD946EF);     // Magenta
}
